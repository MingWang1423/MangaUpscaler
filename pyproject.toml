[tool.ruff]
target-version = "py311"
line-length = 120
# 不检查构建产物、虚拟环境与外部工具等目录
extend-exclude = [".git", ".venv", "venv", "build", "dist", "tools", "_internal"]

[tool.ruff.lint]
# 只启用明确、低争议的规则：语法错误（E9）与 Pyflakes（F：未定义名、未使用导入等）。
# 暂不启用 E501（行长）与格式化类规则，避免制造大面积无关改动。
select = ["E9", "F"]
